# Music Room App 운영/유지보수 문서

작성일: 2026-05-26  
대상: 비개발자 운영자/관리자

## 1) 이 파일들의 역할

### `lib/screens/rooms/room_list_screen.dart`
- 앱 첫 화면(방 목록)입니다.
- 상단 사람 아이콘(계정 버튼)을 눌렀을 때 로그인/관리자/로그아웃 메뉴를 보여줍니다.
- 관리자면 관리자 대시보드(`/admin`)로 이동하고, 로그아웃도 여기서 처리합니다.

### `lib/core/router.dart`
- "누가 어느 화면에 갈 수 있는지"를 정하는 교통정리 파일입니다.
- 로그인 상태에서 `/login`으로 가면 다시 `/`로 보내는 규칙이 있습니다.
- 관리자 전용 화면(`/admin`) 접근 권한도 여기서 확인합니다.

### `lib/screens/auth/login_screen.dart`
- 로그인 입력 화면입니다.
- 뒤로가기 처리 방식이 잘못되면 크래시가 날 수 있는 민감한 화면입니다.

## 2) 꼭 알아야 할 개념 3개

### 1. 로그인 상태 (`user: true/false`)
- `user: true` = 이미 로그인된 상태
- `user: false` = 로그아웃 상태
- 이미 로그인된 상태에서는 로그인 화면으로 가려고 해도 라우터가 되돌릴 수 있습니다.

### 2. 권한(`role`)
- Firestore `users/{uid}` 문서의 `role`이 중요합니다.
- `role == 'admin'`이면 관리자 메뉴/화면 접근 가능
- 문자열 오타(`amin`, `Admin`)가 나면 관리자 기능이 안 보입니다.

### 3. 화면 이동 방식 (`go` vs `push`)
- `context.go('/...')`: 현재 화면을 교체 이동 (메인 전환에 적합)
- `context.push('/...')`: 화면을 위에 쌓아 이동 (로그인 같은 보조 화면)
- 이 둘을 잘못 섞으면 뒤로가기 에러가 발생할 수 있습니다.

## 3) 내가 수정하면 위험한 부분

- `role == 'admin'` 비교 문자열
- 로그아웃 후 이동 흐름(`signOut` -> `/login`)
- 로그인 화면 뒤로가기 방식(`Navigator.pop`로 바꾸면 재크래시 위험)
- 라우터의 `/login` 리다이렉트 규칙

## 4) 자주 나는 에러와 원인

### 에러 A
- 메시지: `You have popped the last page off of the stack`
- 원인: 더 이상 pop할 화면이 없는데 `Navigator.pop` 실행
- 해결: 로그인 화면 뒤로가기를 `context.go('/')`로 처리

### 에러 B
- 메시지: `!_debugLocked`
- 원인: 화면 전환(pop/go)이 같은 타이밍에 겹침
- 해결: 중복 전환 제거, pop 최소화

### 에러 C
- 증상: "로그인 버튼 눌러도 로그인 안 되는 것 같음"
- 실제 원인: 이미 로그인(`user:true`)이라 `/login` 접근 시 라우터가 `/`로 복귀시킴
- 해결: 계정 버튼을 로그인 버튼이 아닌 "계정 메뉴"로 동작

## 5) 다음 점검 체크리스트

- [ ] 앱 실행 로그에서 `user: true/false` 확인
- [ ] 사람 아이콘 클릭 시 상태별 메뉴가 맞게 뜨는지 확인
- [ ] 관리자 계정에서 "관리자 대시보드" 메뉴가 보이는지 확인
- [ ] 로그아웃 후 `/login`으로 이동되는지 확인
- [ ] 로그인 화면에서 뒤로가기 시 크래시가 없는지 확인

## 6) 구조 / 파일 역할 / 데이터 흐름

### 구조
- 홈(방 목록) -> 계정 버튼 -> 권한 조회 -> 메뉴 노출 -> 이동/로그아웃

### 데이터 흐름
1. 사용자가 사람 아이콘 클릭
2. `FirebaseAuth.currentUser`로 로그인 여부 확인
3. 로그인 상태면 `validateAndGetUser(uid)`로 사용자 문서 조회
4. `role == 'admin'`이면 관리자 메뉴 노출
5. 로그아웃 선택 시 `signOut()` 후 `/login` 이동

### 수정 주의사항
- 관리자 판별 문자열은 반드시 `'admin'`
- 로그인 상태에서 `/login` 강제 이동 로직 추가 금지
- 인증/메인 전환에 `Navigator.pop(context)` 남용 금지
- 라우터 리다이렉트 규칙을 임의로 삭제하지 말 것

## 7) 변경 이력 (날짜 기록)

### 2026-06-12
- [x] 독립 HTML 방문 예약 페이지의 Samsung Internet 진입 재확인
    - 현재 PC Wi-Fi IPv4는 `10.101.66.43`으로 확인됨.
    - Samsung Internet에서도 방 상세의 방문 예약 버튼을 통해 `visit_request.html?roomId=...` 진입이 정상 동작함.
    - 이전에 남아 있던 진입 실패는 `returnUrl` 쿼리 시도, 캐시/세션, 또는 네트워크 주소 변경 영향 가능성이 있음.
- [x] HTML 방문 예약 페이지 뒤로가기 동작 보완
    - `returnUrl` 쿼리는 다시 사용하지 않음.
    - `roomId`만 사용해 방 상세 경로(`/#/rooms/{roomId}`)로 복귀하도록 정리.
    - 정상적으로 방 상세에서 들어온 경우에는 `history.back()`을 먼저 시도하고, 직접 접속 등 예외 상황에서는 방 상세 URL로 직접 이동.
- [x] HTML -> Flutter 복귀 속도 확인
    - `flutter run -d web-server` 개발 서버에서는 HTML에서 Flutter 화면으로 돌아갈 때 느릴 수 있음.
    - `flutter build web --release` 후 `python -m http.server 8082 --bind 0.0.0.0`로 정적 서빙했을 때 복귀 속도가 더 빠르게 확인됨.
    - 실제 속도 판단은 개발 서버가 아니라 release build 기준으로 확인하는 것이 적절함.
- [x] Chrome 흰 화면 / 캐시 이슈 확인
    - Chrome에서 메인(`/`)만 흰 화면이고 `visit_request.html?roomId=test`는 열리는 상황이 있었음.
    - 이 경우 네트워크/서버 문제가 아니라 Flutter Web 캐시, 사이트 데이터, 개발 서버 상태 문제로 먼저 판단.
    - Chrome의 `10.101.66.43` 사이트 데이터 삭제 후 Flutter 개발 서버를 재시작하니 정상 접속 확인.
    - 같은 증상이 반복되면 사이트 데이터 삭제, 서버 재시작, `?v=...` 캐시 우회 순서로 확인.
- [x] 방 상세 / 방문 예약 HTML 네비게이션 보완
    - 방 상세 화면 좌측 뒤로가기 버튼은 `canPop()` 가능하면 뒤로가고, 스택이 없으면 `/`로 이동.
    - 방 상세 화면 우측 상단에 홈 버튼 추가.
    - 방문 예약 HTML 화면 우측 상단에도 동일한 홈 버튼 추가.
    - 방문 예약 버튼은 현재 접속 중인 IP/포트 기준 절대 URL로 `visit_request.html?roomId=...` 이동하도록 보완.
- [x] 방문 예약 HTML 폼 UX 보완
    - 성함 placeholder: `성함을 입력하세요`.
    - 연락처 placeholder: `연락처를 입력하세요`.
    - 연락처 입력 시 `010-` 자동 시작 및 `010-1234-5678` 형태로 표시. 저장/검증은 숫자만 사용.
    - 방문 희망일은 자체 달력 모달로 변경. 상단에 `취소`, `설정`만 표시하고 삭제 버튼은 사용하지 않음.
    - 날짜 선택 전 방문 희망 시간 영역에 `방문 날짜 선택 후 활성화됩니다` 안내 표시.
    - 예약 저장 성공 시 브라우저 기본 alert 대신 자체 완료 모달 표시. 확인 누르면 메인(`/`)으로 이동.
- [ ] 로그인 ID 방식 추가 고민
    - 현재 로직상 문자 ID는 `admin@yourroom.com` 형태로 로그인 가능.
    - 고객은 휴대폰 뒤 8자리만 입력해도 `010`이 붙도록 처리되어 있음.
    - 로그인 화면 UI는 전화번호 중심이라 관리자 ID 노출 방식은 추후 결정.
- [ ] 다음 커밋/정리 때 확인할 파일
    - `web/visit_request.html`은 독립 HTML 방문 예약 페이지이므로 반드시 함께 관리해야 함.
    - 방문 예약 HTML 관련 변경을 커밋할 때 `README.md`, `README_OPERATIONS.md`, `web/visit_request.html`을 같이 확인.
    - `returnUrl` 방식은 재도입하지 말 것. 방 상세 복귀는 `roomId` 기반으로 처리.
    - HTML 진입 URL은 계속 단순한 `visit_request.html?roomId=...` 형태를 유지.

### 2026-06-02
- [x] 모바일 Chrome 한글 입력 엑스박스/네모 표시 문제 대응
    - web/index.html의 Flutter Web HTML renderer 강제 설정 제거
    - Pretendard 폰트 추가 및 전역 ThemeData fontFamily 적용
    - 방문예약 입력 화면의 한글 표시 안정성 개선
- [x] 방문예약 성함 입력 중 한글 조합 깨짐/잔상 문제 추가 대응
    - 성함 TextFormField에 모바일 웹 입력 안정화 옵션 적용
    - autocorrect, suggestions, smart dash/quote 등 불필요한 자동 보정 비활성화
    - 한글 입력 제한 inputFormatter는 추가하지 않음
- [x] 방 목록 사진 미리보기 제거
    - 방 목록 카드 상단의 사진/회색 placeholder 영역 제거
    - 게스트/사용자/관리자 모두 방 목록은 텍스트 정보 중심 리스트로 표시
    - 방 상세 화면의 사진 PageView 기능은 유지

### 2026-05-27
- [x] 관리자 방문예약 화면을 리스트형 이력 조회로 개선 (대기/확정/취소/전체 필터)
- [x] pending 상태에서만 확정/취소 버튼 노출, 처리 완료 건은 버튼 숨김
- [x] 상태별 색상 배지 및 빈 목록 안내 문구 추가
- [x] 방문예약 상태 변경 시 timeout 1회 자동 재시도(700ms 후)
- [x] 권한/네트워크/지연 오류 메시지 분리 및 스낵바 안내 강화
- [x] visitId를 doc.id 기반으로 안정 매핑하여 상태 변경 실패 케이스 감소

### 2026-05-26
- 홈 계정 버튼을 상태 기반 메뉴로 정리
  - 비로그인: 로그인
  - 관리자: 관리자 대시보드 + 로그아웃
  - 일반 로그인: 로그아웃
- 관리자 계정 로그인 시 `/admin` 진입 확인
- 로그아웃 후 로그인 화면 뒤로가기 크래시 대응
  - 원인: pop 가능한 페이지 없음
  - 조치: 로그인 화면 뒤로가기 동작 안정화(`go` 기반)
- 통계/설정 관련 보완(별도 작업)
  - 월 통계 계산 조건/예외 처리 보강
  - 방문예약 설정 실시간 반영/시간 검증/기본값 보강

---

운영 팁:
- "로그인이 안 된다"고 느껴질 때는 먼저 로그의 `user:true/false`를 확인하세요.
- `user:true`면 로그인이 안 된 게 아니라 이미 로그인된 상태입니다.
---

## 2026-06-10 추가 작업 메모

- 관리자 대시보드 방 현황 필터 추가
  - 상단 `전체`, `공실`, `계약중` 숫자를 누르면 아래 방 목록이 해당 상태로 필터되도록 변경.
  - `_RoomStatusTab`을 상태 위젯으로 전환하고 선택 필터 상태를 유지.

- 모바일 웹 한글 입력 문제 재검토
  - Android Chrome 모바일 웹에서 Flutter `TextFormField` 한글 입력 시 조합 중 글자 깨짐과 재포커스 후 백스페이스 미동작이 확인됨.
  - 신규 계약자 등록 화면의 기본 `TextFormField`에서도 동일 증상이 재현되어 특정 방문 예약 코드 문제가 아닌 Flutter Web + Android Chrome 한글 IME 문제로 판단.
  - `HtmlElementView`/HTML input 삽입 방식은 입력 안정성은 개선되나 Flutter 화면 내부에서는 Android 다크모드로 입력 배경이 검게 보이는 한계가 있었음.

- 방문 예약 독립 HTML 폼 실험 추가
  - `web/visit_request.html` 추가.
  - 방 상세 화면의 방문 예약 버튼을 `visit_request.html?roomId=...`로 이동하도록 변경.
  - 기존 Flutter 방문 예약 화면은 비교와 롤백을 위해 유지.
  - 목표는 Flutter Web 텍스트 입력 레이어를 우회하면서 흰 배경/검은 글씨 디자인과 한글 입력 안정성을 동시에 검증하는 것.

### 2026-06-10 중단 시점 / 다음 확인 사항

> 2026-06-12 업데이트: 아래는 당시 중단 시점 기록입니다. 현재는 Samsung Internet 진입이 정상 확인됐고, `returnUrl` 없이 `roomId` 기반 뒤로가기 방식으로 보완했습니다.

- 독립 HTML 방문 예약 페이지 상태
  - 직접 URL 접속은 가능: `http://10.207.59.43:8081/visit_request.html?roomId=test`
  - PC Chrome에서는 방 상세의 방문 예약 버튼으로 진입 가능.
  - 모바일 Chrome에서도 방문 예약 신청 페이지까지 이동되는 것으로 확인.
  - Samsung Internet에서는 방 상세의 방문 예약 버튼 클릭 시 HTML 페이지로 진입하지 못하고 튕기는 문제가 남음.

- 시도했다가 되돌린 내용
  - 뒤로가기 버튼이 방 목록으로 빠지는 문제를 고치기 위해 `returnUrl` 쿼리를 붙이는 방식을 시도함.
  - Flutter Web 해시 라우팅(`#/rooms/...`)과 쿼리 인코딩이 섞이면서 Samsung Internet 진입 문제가 시작된 것으로 추정.
  - `returnUrl` 관련 변경은 제거했고, 방 상세 버튼 이동 URL은 다시 단순한 `visit_request.html?roomId=...` 형태로 되돌림.
  - `browser_navigation_*` 유틸을 추가해 웹에서 `window.location.href`로 직접 이동하는 방식도 시도했으나 문제 해결이 확인되지 않아 제거함.

- 다음 작업 우선순위
  1. Samsung Internet에서 버튼 클릭 직후 실제 주소창 URL 또는 에러 상태 확인.
  2. `url_launcher(webOnlyWindowName: '_self')` 대신 Flutter 위젯 트리에 실제 링크(`<a href>`)에 가까운 방식으로 렌더링하는 방법 검토.
  3. 방문 예약 HTML 페이지 진입 안정화를 먼저 해결하고, 뒤로가기 방 상세 복귀는 이후 별도 단계로 처리.
  4. 독립 HTML 폼의 한글 입력/백스페이스 안정성은 직접 URL 접속 기준으로 먼저 검증.
---

## 2026-06-29 운영 변경 메모

- 신규 계약자 등록 화면에 `보증금 (원)` 입력란을 추가했습니다.
  - 방을 선택하면 해당 방의 기존 `deposit` 값이 기본값으로 들어갑니다.
  - 계약 완료 시 선택한 방 문서(`rooms/{roomId}`)의 `deposit` 값도 입력값으로 함께 갱신됩니다.
- 신규 계약자 연락처 입력란은 처음부터 `010`이 들어간 상태로 시작합니다.
  - 계약 등록 후 폼을 초기화해도 연락처는 다시 `010`으로 돌아갑니다.
- 신규 계약자 등록 화면의 월세/납부일/보증금 입력칸은 Flutter Web 숫자 input 편집 문제를 피하기 위해 `TextInputType.text` + 숫자만 허용 formatter로 처리합니다.
  - 방 선택 시 월세와 보증금은 `280000`처럼 콤마 없는 숫자로 채웁니다.
  - 운영자가 기존 금액을 백스페이스로 끝까지 지운 뒤 새 금액을 입력할 수 있습니다.
- 검증/배포 기록:
  - 사용자 로컬 검증: `flutter build web` 성공.
  - 커밋: `1b25a9c fix: improve new customer payment inputs`
  - Firebase Hosting 배포: `https://music-room-app-a65c7.web.app`
